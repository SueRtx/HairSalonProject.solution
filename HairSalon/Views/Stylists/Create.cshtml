@{
  Layout = "_Layout";
}

<div class="container">
  <h1 class="title"><strong>EAU CLAIRE'S SALON</strong></h1>
  <h2>Add Stylist</h2>
  
  <div class="infoBox">
    @model HairSalon.Models.Stylist
    
    @using (Html.BeginForm())
    {
      <p>@Html.Label("Name:") 
      @Html.TextBoxFor(model => model.Name, new { required = "required" })</p>

      <p>@Html.LabelFor(model => model.Specialty): 
      @Html.TextBoxFor(model => model.Specialty, new { required = "required" })</p>

      <p>@Html.Label("Hired Date:") 
      @Html.TextBoxFor(model => model.DateHired, new { required = "required", @type = "date"})</p>
      
      <input type="submit" value="Add Stylist" id="btn2" />
    }
  </div>
    <h4 id="leftSidetLink">@Html.ActionLink("Stylist List", "Index")</h4>
    <h4 id="rightSideLink">@Html.ActionLink("Return Home", "Index", "Home")</h4>
</div>